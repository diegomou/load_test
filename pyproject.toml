[project]
name = "load-test"
version = "0.1.0"
description = "Load testing tool for stress testing websites"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "locust>=2.43.3"
]

[project.optional-dependencies]
dev = []